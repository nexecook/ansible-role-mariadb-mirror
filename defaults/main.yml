---

mariadb_mirror_cron_disabled: true
mariadb_mirror_source: 'rsync.osuosl.org::mariadb/'
mariadb_mirror_cron_minute: "{{ 59 | random(seed=inventory_hostname) }}"
mariadb_mirror_cron_hour: "{{ 23 | random(seed=inventory_hostname) }}"
